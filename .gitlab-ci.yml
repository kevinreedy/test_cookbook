image: "kevinreedy/chefdk-debug:0.0.4"

# TODO
# cache:

services:
  - docker:dind

before_script:
  - apt-get update && apt-get -y install rsync
  - chef --version

test:
  script:
    - cookstyle
    - foodcritic --exclude test --epic-fail any .
    - kitchen test
